<%= simple_form_for [cocktail] do |f| %>
  <%= f.input :name %>
  <%= f.association :ingredients, collection: Ingredient.all, as: :check_boxes %>
  <%= f.submit "create a cocktail", class: "btn btn-primary" %>
<% end %>
